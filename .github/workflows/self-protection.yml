name: self-protection

on:
  workflow_dispatch:
  push:
    branches: ["main", "feat/eguard-agent"]
  release:
    types: [published]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify anti-tamper contracts
        run: ./scripts/run_self_protection_verification_ci.sh
