title: windows_registry_runkey_persistence
id: windows_registry_runkey_persistence
status: experimental
logsource:
  product: windows
  category: process_creation
tags:
  - attack.t1547
  - attack.t1112
mitre_techniques:
  - T1547
  - T1112

detection:
  sequence:
    - event_class: process_exec
      process_any_of:
        - reg.exe
      command_line_contains:
        - reg add
        - CurrentVersion\\Run
      within_secs: 30
